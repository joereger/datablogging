<project name="RegercomDistributable" default="buildWar" basedir=".">
  <description>
        The Best Darn Build Process This Side of Sanity
  </description>

  <!-- Import properties file -->
  <property file="RegercomDistributable.properties"/>
  
  <!-- Build the WAR File -->
  <target name="buildWar" description="Create a WAR file.">
    <ant antfile="MainBuild.xml" target="buildWar" inheritAll="true"/>
  </target>

    <!-- Build the WAR File -->
    <target name="buildObfuscated" description="Create an obfuscated jar file.">
        <ant antfile="MainBuild.xml" target="buildObfuscated" inheritAll="true"/>
    </target>

    <!-- Build the WAR File -->
    <target name="buildRegerCoreJar" description="Create the jar file.">
        <ant antfile="MainBuild.xml" target="createregerjar" inheritAll="true"/>
    </target>

    <!-- Build JavaDoc -->
    <target name="createJavaDoc" description="Create javaDoc for this project.">
        <ant antfile="MainBuild.xml" target="createJavaDoc" inheritAll="true"/>
    </target>

</project>

