<%

if (request.getParameter("accountid")!=null && reger.util.isinteger(request.getParameter("accountid"))){
    String rootUrl = reger.Account.getSiteRootUrlViaAccountid(Integer.parseInt(request.getParameter("accountid")));
    try {
        response.sendRedirect(reger.Vars.getHttpUrlPrefix() + rootUrl + "/");
        return;
    } catch (Exception e){
        reger.util.errorsave(e);
    }
    out.print("Please click <a href='" + reger.Vars.getHttpUrlPrefix() + rootUrl + "/'>here</a>.");
} else {
    out.print("Site not found.  Please try again.");    
}

%>